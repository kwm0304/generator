package com.kwm0304.cli.service;

import com.kwm0304.cli.model.ModelField;

import java.util.ArrayList;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class ParserService {
//    private String parseClassName(String content) {
//        Pattern pattern = Pattern.compile("public class (\\w+)");
//        Matcher matcher = pattern.matcher(content);
//        if (matcher.find()) {
//            return matcher.group(1);
//        }
//        return null;
//    }
//
//    private List<ModelField> parseFields(String content) {
//        List<ModelField> fields = new ArrayList<>();
//        Pattern pattern = Pattern.compile("private (\\w+) (\\w+);");
//        Matcher matcher = pattern.matcher(content);
//
//        while(matcher.find()) {
//            String type = matcher.group(1);
//            String name = matcher.group(2);
//
//            boolean isId = name.equals()
//        }
//    }
}
